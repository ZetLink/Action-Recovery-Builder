<?xml version="1.0" encoding="UTF-8"?>
<manifest>
  <!--remote name="{REMOTE_NAME}"
          fetch="https://{REMOTE_FETCH}"/-->

  <!-- Device tree -->
  <!--project name="{DEVICE_TREE_URL_REPO}"
           path="{DEVICE_PATH}"
           remote="{DEVICE_REMOTE_NAME}"
           revision="{DEVICE_TREE_BRANCH}"/-->

  <!-- Kernel -->
  <!--project name="{DEVICE_KERNEL_URL_REPO}"
           path="{KERNEL_PATH}"
           remote="{KERNEL_REMOTE_NAME}"
           revision="{DEVICE_KERNEL_BRANCH}"/-->
</manifest>
